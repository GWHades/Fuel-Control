# Fuel Control (Frontend PWA)

## Local run
1. Copie `.env.example` para `.env` e configure a API:

```bash
VITE_API_URL=http://127.0.0.1:8000
```

2. Rode:

```bash
npm install
npm run dev
```

Abra: http://localhost:5173
